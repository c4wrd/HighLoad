package com.c4wrd.loadtester.request;

import com.opencsv.CSVReader;

import java.io.*;
import java.util.List;

public class QueryParamFactory {

    /*public List<QueryCombinations> buildFromCsv(InputStream fileStream) {
        CSVReader reader = new CSVReader()
    }

    public static void main(String[] args) throws IOException {
        CSVReader reader = new CSVReader(new InputStreamReader(ClassLoader.getSystemResourceAsStream("data.csv")));
        List<String[]> data = reader.readAll();

        System.out.println(data.parallelStream()
                .map(item -> {
                    return item[1];
                })
                .distinct()
                .count());
    }*/
}
